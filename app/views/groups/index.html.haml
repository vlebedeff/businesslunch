.row.margin-bottom
  .col-md-12
    %h1 Groups

.row.subnav
  .col-md-12
    - @groups.each do |group|
      %div{id: dom_id(group)}
        = group.name
        - if can? :join, group
          = link_to 'Join', join_group_path(group), method: :post, class: 'btn btn-success'
