%h3
  = @report.from
  = "-"
  = @report.to
%p
  %section.orders
    %ul
      %li.header
        .col-md-4 Date
        .col-md-4 Count
      - if @report.orders.any?
        - DictionaryPresenter.new(OrderPolicy.new, @report.orders).as_dictionary.each do |date, orders|
          %li
            .col-md-4= date
            .col-md-4= orders.count
      - else
        %li
          .col-md-12 There is no orders for selected period
      %li.total
        .col-md-4 Total
        .col-md-4= @report.total
